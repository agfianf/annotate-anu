version: '3.8'

services:
  # Backend - SAM3 FastAPI Application
  backend:
    build:
      context: ./apps/api-inference
      dockerfile: Dockerfile
    # images: sam3-be
    container_name: anu-backend-inference
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - ./apps/api-inference/src:/code/src  # Mount source for hot-reload
      - huggingface_cache:/root/.cache/huggingface  # Cache model downloads
    env_file:
      - ./apps/api-inference/.env
    restart: unless-stopped
    networks:
      - anu-network
    # Uncomment for GPU support (requires nvidia-docker)
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  # API Core - Model Registry Service
  api-core:
    build:
      context: ./apps/api-core
      dockerfile: Dockerfile
    container_name: anu-api-core
    ports:
      - "8001:8001"
    volumes:
      - ./apps/api-core/src:/code/src
      - api-core-data:/code/data
    env_file:
      - ./apps/api-core/.env
    restart: unless-stopped
    networks:
      - anu-network
    depends_on:
      - redis

  # Redis - Caching Layer
  redis:
    image: redis:7-alpine
    container_name: anu-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - anu-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  # Frontend - React Annotation Platform
  frontend:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
      target: development
    # images: sam3-fe
    container_name: anu-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./apps/web:/app
      - /app/node_modules
    env_file:
      - ./apps/web/.env
    depends_on:
      - backend
      - api-core
    networks:
      - anu-network
    command: npm run dev -- --host 0.0.0.0

  # Frontend Production (uncomment for production)
  # frontend-prod:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: sam3-frontend-prod
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - backend
  #   networks:
  #     - sam3-network
  #   restart: unless-stopped

volumes:
  huggingface_cache:
    driver: local
  api-core-data:
    driver: local
  redis-data:
    driver: local

networks:
  anu-network:
    driver: bridge
