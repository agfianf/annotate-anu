# AGENTS.md

Repository instructions live in `CLAUDE.md` at the repo root; read that file first. Path-scoped rules are in `.claude/rules/frontend.md` (for `apps/web/**`) and `.claude/rules/backend.md` (for the two API apps).
